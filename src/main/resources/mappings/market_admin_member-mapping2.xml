<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="marketAdminDAO">

	<!-- 매장관리자 아이디 값 파라미터로 받기 -->
	<select id="checkMemberId" parameterType="string" resultType="com.damino.web.admin.market.member.login.MarketAdminVO">
	SELECT * FROM MarketAdminMember WHERE managerId=#{managerId}
	</select>
	
</mapper>



