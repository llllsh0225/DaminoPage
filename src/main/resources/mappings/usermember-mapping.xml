<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="UserMemberDAO">
	<!-- 회원 가입 -->
	<insert id="registUserMember" parameterType="com.damino.web.user.regist.UserMemberVO">
		INSERT INTO usermember (username, userid, userpasswd, birthday, sex, cyear, phone, email, receive_sms, receive_email, receive_dm, userlevel) 
		VALUES (#{username}, #{userid}, #{userpasswd}, #{birthday}, #{sex}, #{cyear}, #{phone}, #{email}, #{receive_sms}, #{receive_email}, #{receive_dm}, 'REGULAR' )
	</insert>
</mapper>



