<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="TermsDAO">
	
	<select id="getTermsList" resultType="com.damino.web.admin.terms.TermsVO">
		SELECT seq, title, subtitle, regdate, flag FROM terms 
	</select>
	
	<select id="getTerms" resultType="com.damino.web.admin.terms.TermsVO">
		SELECT seq, title, subtitle, regdate, flag FROM terms WHERE seq=#{seq}
	</select>
	
	<!-- <resultMap type="HashMap" id="result">
		<result property="content" column="content" jdbcType="CLOB" javaType="java.lang.String"/>
	</resultMap>
	<select id="getContent" parameterType="HashMap" resultType="result">
		SELECT content FROM terms WHERE seq=#{seq}
	</select> -->
</mapper>
